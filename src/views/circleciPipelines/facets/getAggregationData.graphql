query($field: String!, $query: String) {
  circleciPipelines {
    data(query: $query) {
      aggregations(field: $field) {
        field
        buckets {
          docCount
          key
        }
      }
    }
  }
}
