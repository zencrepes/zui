query($field: String!, $query: String) {
  githubMavenPoms {
    data(query: $query) {
      metrics(field: $field) {
        field
        overallMin
        overallMax
        min
        max
      }
    }
  }
}
