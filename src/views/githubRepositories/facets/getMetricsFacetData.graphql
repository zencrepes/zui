query($field: String!, $query: String) {
  githubRepositories {
    data(query: $query) {
      metrics(field: $field) {
        field
        overallMin
        overallMax
        min
        max
      }
    }
  }
}
