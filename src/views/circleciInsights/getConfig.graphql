query {
  dataset: circleciInsights {
    config {
      table {
        itemsType
        columns {
          name
          field
          sortField
          sortable
        }
      }
      aggregations {
        totalCount
        nodes {
          name
          field
          facetType
          default
          nullValue
          nullFilter
        }
      }
    }
  }
}
