query($field: String!, $query: String) {
  testingCases {
    data(query: $query) {
      metrics(field: $field) {
        field
        overallMin
        overallMax
        min
        max
      }
    }
  }
}
