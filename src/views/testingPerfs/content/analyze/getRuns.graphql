query($query: String) {
  testingPerfs {
    data(query: $query) {
      items(size: 100) {
        nodes {
          id
          name
          rampUp
          startedAt
        }
      }
    }
  }
}

