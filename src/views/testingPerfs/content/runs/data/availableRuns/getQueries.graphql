query($query: String) {
  testingPerfs {
    data(query: $query) {
      items(size: 1000) {
        nodes {
          id
          name
          rampUp
          startedAt
        }
      }
    }
  }
}
