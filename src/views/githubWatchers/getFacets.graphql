query {
  githubWatchers {
    config {
      aggregations {
        totalCount
        nodes {
          name
          field
          facetType
          default
          nullValue
          nullFilter
        }
      }
    }
  }
}
