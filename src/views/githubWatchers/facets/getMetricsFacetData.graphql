query($field: String!, $query: String) {
  githubWatchers {
    data(query: $query) {
      metrics(field: $field) {
        field
        overallMin
        overallMax
        min
        max
      }
    }
  }
}
