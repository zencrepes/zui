query($field: String!, $query: String) {
  githubWatchers {
    data(query: $query) {
      aggregations(field: $field) {
        field
        buckets {
          docCount
          key
        }
      }
    }
  }
}
