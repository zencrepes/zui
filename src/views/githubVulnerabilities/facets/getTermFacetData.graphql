query($field: String!, $query: String) {
  githubVulnerabilities {
    data(query: $query) {
      aggregations(field: $field) {
        field
        buckets {
          docCount
          key
          count
        }
      }
    }
  }
}
