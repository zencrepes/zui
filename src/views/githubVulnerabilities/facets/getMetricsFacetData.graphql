query($field: String!, $query: String) {
  githubVulnerabilities {
    data(query: $query) {
      metrics(field: $field) {
        field
        overallMin
        overallMax
        min
        max
      }
    }
  }
}
