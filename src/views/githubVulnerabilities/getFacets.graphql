query {
  githubVulnerabilities {
    config {
      aggregations {
        totalCount
        nodes {
          name
          field
          facetType
          default
        }
      }
    }
  }
}
